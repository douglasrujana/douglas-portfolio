---
/**
 * Astro Island for the Chat Widget.
 * This component acts as a server-rendered container that loads the
 * interactive Svelte component on the client when it becomes visible.
 */
import ChatWidget from '@presentation/components/chat/ChatWidget.svelte';
---
<ChatWidget client:visible />